# Core dependencies.
#
# torch is installed from PyPI by default, which pulls the CUDA build on Linux.
# For a CPU-only install (much smaller, no NVIDIA wheels), install torch first:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
# For a specific CUDA build, swap /cpu for /cu124 (or whatever matches your driver).
torch>=2.1
transformers>=4.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7

# Testing
pytest>=7.4

# Optional: the zero-shot LLM row only.
# Deliberately not installed by default -- that row is opt-in and bills per
# validation row. Install it when you actually intend to run one:
#   pip install anthropic
